apiVersion: v1
kind: Service
metadata:
  name: airsel-micro-webapp
  labels:
    service: airsel-micro-webapp
  name: airsel-micro-webapp
spec:
  selector:
    app: airsel-micro-webapp
  ports:
  - port: 80
    targetPort: 80